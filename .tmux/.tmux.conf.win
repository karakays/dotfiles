# copy-mode bindings
bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "putclip"

# panes
set-option -g pane-active-border-style bg=default,fg=yellow

set-option -g status-right '#[bg=black,fg=colour251,nobright, bold] #(ipad || echo 0.0.0.0) '
set-option -ag status-right '#[bg=default]📅 %d-%b %H:%M'

set-option -g status-left '#[bg=colour1,fg=colour15] #H #[bg=default] '

