[user]
	email           = skarakayali@gmail.com
	name            = Selçuk Karakayalı
    signingkey      = skarakayali@gmail.com
[core]
    editor          = vim
    excludesfile    = .gitignore_global
    autocrlf        = false
    filemode        = false
    sshCommand      = ssh -o IdentitiesOnly=yes -i ~/.ssh/personal_ed25519
[commit]
	gpgsign = true
[push]
    autoSetupRemote = true
[alias]
    ci              = commit -S
    co              = checkout
    cla             = clone --recurse-submodules
    logp            = log --all --decorate --abbrev-commit --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset'
    st              = status
    diffs           = diff --staged
    main            = !git checkout main && git pull origin main && git checkout -
[gpg]
	format          = ssh
[color]
    ui              = true
[user]
	signingkey      = ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIC7mbgO518ZwoqvI6KB1GgDbLiBnbPdkSDOSIakHEPEA skarakayali@gmail.com
[includeIf "gitdir:~/Code/"]
    path            = ~/workspace/dotfiles/git/work.config
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
